# Copyright 2026 Specter Ops, Inc.
#
# Licensed under the Apache License, Version 2.0
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0

parameters:
  - $ref: './../parameters/header.prefer.yaml'
get:
  operationId: ListCollectorJobSecrets
  summary: List collector job secret references
  tags:
    - Collector Jobs
    - Enterprise
  parameters:
    - $ref: './../parameters/query.skip.yaml'
    - $ref: './../parameters/query.limit.yaml'
    - name: type
      in: query
      schema:
        $ref: './../schemas/api.params.predicate.filter.string.yaml'
    - name: display_key_id
      in: query
      schema:
        $ref: './../schemas/api.params.predicate.filter.string.yaml'
    - $ref: './../parameters/query.created-at.yaml'
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            allOf:
              - $ref: './../schemas/api.response.pagination.yaml'
              - type: object
                required:
                  - data
                properties:
                  data:
                    type: object
                    required:
                      - secrets
                    properties:
                      secrets:
                        type: array
                        items:
                          $ref: './../schemas/model.collector-job-secret.yaml'
    400:
      $ref: './../responses/bad-request.yaml'
    401:
      $ref: './../responses/unauthorized.yaml'
    403:
      $ref: './../responses/forbidden.yaml'
    429:
      $ref: './../responses/too-many-requests.yaml'
    500:
      $ref: './../responses/internal-server-error.yaml'
post:
  operationId: CreateCollectorJobSecret
  summary: Create a collector job secret reference
  tags:
    - Collector Jobs
    - Enterprise
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          required:
            - type
            - key_id
            - display_key_id
            - value
          properties:
            type:
              type: string
              enum:
                - auth_key
            key_id:
              type: string
            display_key_id:
              type: string
            value:
              type: string
              writeOnly: true
              description: Secret material stored in the external secret manager and never returned by this API.
  responses:
    201:
      description: Created
      content:
        application/json:
          schema:
            type: object
            required:
              - data
            properties:
              data:
                type: object
                required:
                  - secret
                properties:
                  secret:
                    $ref: './../schemas/model.collector-job-secret.yaml'
    400:
      $ref: './../responses/bad-request.yaml'
    401:
      $ref: './../responses/unauthorized.yaml'
    403:
      $ref: './../responses/forbidden.yaml'
    429:
      $ref: './../responses/too-many-requests.yaml'
    500:
      $ref: './../responses/internal-server-error.yaml'
