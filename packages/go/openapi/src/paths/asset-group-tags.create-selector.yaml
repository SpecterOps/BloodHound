parameters:
  - $ref: './../parameters/header.prefer.yaml'
  - name: asset_group_tag_id
    description: ID of the asset_group_tag to create a selector
    in: path
    required: true
    schema:
      type: integer
      format: int32

post:
  operationId: CreateAssetGroupTagSelector
  summary: Create Asset Group Tag Selector
  description: Creates an asset group tag selector.
  tags:
    - Asset Group Management
    - Community
    - Enterprise
  requestBody:
    description: The request body for creating an asset group tag selector
    required: true
    content:
      application/json:
        schema:
          type: object
          items:
            $ref: './../schemas/model.asset-group-tags-selector.yaml'
  responses:
    201:
      description: Created
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  type: array
                  items:
                    $ref: './../schemas/model.asset-group-selector.yaml'

    400:
      $ref: './../responses/bad-request.yaml'
    404:
      $ref: './../responses/not-found.yaml'
    500:
      $ref: './../responses/internal-server-error.yaml'
