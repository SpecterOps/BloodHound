# Copyright 2026 Specter Ops, Inc.
#
# Licensed under the Apache License, Version 2.0
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0
post:
  operationId: SplitPrivilegeZonesByEnvironment
  summary: Preview or enable persistent environment-scoped Tier Zero mode
  tags: [Privilege Zone Boundaries, Enterprise]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [environments, dry_run]
          properties:
            environments:
              type: array
              items:
                $ref: './../schemas/model.privilege-zone-environment.yaml'
            dry_run:
              type: boolean
            zone_names:
              type: object
              description: Optional names keyed by environment object ID. Existing zones cannot be renamed by splitting.
              additionalProperties:
                type: string
                minLength: 1
                maxLength: 255
  responses:
    200:
      description: Tier Zero split dry-run plan
      content:
        application/json:
          schema:
            type: object
            required: [data]
            properties:
              data:
                $ref: './../schemas/model.privilege-zone-split-response.yaml'
    201:
      description: Split mode enabled and current Tier Zero zones created
      content:
        application/json:
          schema:
            type: object
            required: [data]
            properties:
              data:
                $ref: './../schemas/model.privilege-zone-split-response.yaml'
    400: { $ref: './../responses/bad-request.yaml' }
    401: { $ref: './../responses/unauthorized.yaml' }
    403: { $ref: './../responses/forbidden.yaml' }
    500: { $ref: './../responses/internal-server-error.yaml' }
delete:
  operationId: CombinePrivilegeZonesByEnvironment
  summary: Disable split mode and restore the shared Tier Zero zone
  tags: [Privilege Zone Boundaries, Enterprise]
  responses:
    204:
      description: Tier Zero zones combined
    401: { $ref: './../responses/unauthorized.yaml' }
    403: { $ref: './../responses/forbidden.yaml' }
    500: { $ref: './../responses/internal-server-error.yaml' }
