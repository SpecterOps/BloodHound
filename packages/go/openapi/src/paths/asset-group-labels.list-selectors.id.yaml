get:
  operationId: GetAssetGroupLabelsSelectors
  summary: Get Asset Group Label selectors
  description: Get a list of selectors for this group.
  tags:
    - Asset Group Management
    - Enterprise
    - Community
  parameters:
  - name: asset_group_label_id
    description: ID of the asset_group_label record to retrieve
    in: path
    required: true
    schema:
      type: integer
      format: int
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            allOf:
              - type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: './../schemas/model.asset-group-label-selectors.yaml'
    400:
      $ref: './../responses/bad-request.yaml'
    401:
      $ref: './../responses/unauthorized.yaml'
    403:
      $ref: './../responses/forbidden.yaml'
    500:
      $ref: './../responses/internal-server-error.yaml'
