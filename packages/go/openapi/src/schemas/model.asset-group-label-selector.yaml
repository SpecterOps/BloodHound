allOf:
  - type: object
    properties:
      object_id:
        type: integer
      asset_group_label_id:
        type: integer
      created_at:
        type: string
        format: date-time
      created_by:
        type: string
      updated_at:
        type: string
        format: date-time
      updated_by:
        type: string
      disabled_at:
        type: string
        format: date-time
      disabled_by:
        type: string
      name:
        type: string
      description:
        type: string
      is_default:
        type: boolean
      auto-certify:
        type: boolean
      seeds:
        type: array
        items:
          $ref: './../schemas/model.asset-group-label-selector-seed.yaml'