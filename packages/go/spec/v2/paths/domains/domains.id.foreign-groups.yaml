parameters:
  - type: string
    description: Object ID
    name: object_id
    in: path
    required: true
get:
  description: List the groups outside this domain that are members of groups in
    this domain
  tags:
    - Domain Entity API
    - Community
    - Enterprise
  summary: List domain foreign groups
  parameters:
    - $ref: './../../parameters/header/prefer.yaml'
    - type: integer
      description: Paging Skip
      name: skip
      in: query
    - type: integer
      description: Paging Limit
      name: limit
      in: query
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "#/definitions/api.ResponseWrapper"
    default:
      $ref: './../../responses/api/error-response.yaml'
