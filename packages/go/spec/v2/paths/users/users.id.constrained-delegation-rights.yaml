parameters:
  - type: string
    description: User Object ID
    name: object_id
    in: path
    required: true
get:
  description: List the computers this user can use constrained delegation rights
    to execute code on
  tags:
    - User Entity API
    - Community
    - Enterprise
  summary: List user constrained delegation rights
  parameters:
    - $ref: './../../parameters/header/prefer.yaml'
    - type: integer
      description: Paging Skip
      name: skip
      in: query
    - type: integer
      description: Paging Limit
      name: limit
      in: query
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "#/definitions/api.ResponseWrapper"
    default:
      $ref: './../../responses/api/error-response.yaml'
