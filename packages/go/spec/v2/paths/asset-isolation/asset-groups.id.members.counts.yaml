parameters:
  - type: integer
    description: Asset group ID
    name: asset_group_id
    in: path
    required: true
get:
  description: List counts of members of an asset isolation group by primary kind
  consumes:
    - application/json
  tags:
    - Asset Isolation API
    - Community
    - Enterprise
  summary: List counts of members of an asset isolation group by primary kind
  parameters:
    - $ref: './../../parameters/header/prefer.yaml'
    - type: string
      description: Filter results by column value. Valid filter predicates are eq,
        neq
      name: object_id
      in: query
      required: false
    - type: string
      description: Filter results by column value. Valid filter predicates are eq,
        neq
      name: environment_id
      in: query
      required: false
    - type: string
      description: Filter results by column value. Valid filter predicates are eq,
        neq
      name: domainsid
      in: query
      required: false
    - type: string
      description: Return counts only for the specified node kind. Valid filter predicates
        are eq, neq
      name: primary_kind
      in: query
      required: false
    - type: string
      description: Filter results by column value. Valid filter predicates are eq,
        neq
      name: environment_kind
      in: query
      required: false
    - type: string
      description: Filter results by column value. Valid filter predicates are eq,
        neq
      name: name
      in: query
      required: false
    - type: string
      description: Filter results by column value. Valid filter predicates are eq,
        neq
      name: custom_member
      in: query
      required: false
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "#/definitions/v2.ListAssetGroupMemberCountsResponse"
    default:
      $ref: './../../responses/api/error-response.yaml'
