import { FC } from 'react';

const PageDescriptionLabels: FC = () => (
    <>Tag groups of objects for easier searching and filtering. Learn how to manage labels to group objects.</>
);

export default PageDescriptionLabels;
